server:
  port: ${SERVER_PORT:8080}
demo:
  enabled: ${DEMO_MODE_ENABLED:false}
spring:
  application:
    name: fast-application
  profiles:
    include:
      - logging
      - redis
      - auth
      - mybatis
      - database
      - upload
      - quartz
      - ai
      - flow
