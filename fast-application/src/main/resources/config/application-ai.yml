spring:
  ai:
    mcp:
      client:
        enabled: ${AI_MCP_CLIENT_ENABLED:true}

ai-assistant:
  enabled: ${AI_ASSISTANT_ENABLED:true}
  require-permission: ${AI_ASSISTANT_REQUIRE_PERMISSION:false}
