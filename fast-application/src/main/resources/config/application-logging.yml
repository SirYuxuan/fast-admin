logging:
  # 日志文件根目录（logback-spring.xml 读取此值）
  file:
    path: ./logs
  level:
    root: info
    cc:
      oofo: debug
    com:
      baomidou: error
  # 控制台格式由 logback-spring.xml 统一管理，此处不再配置 pattern
